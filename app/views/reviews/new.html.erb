<div class="container">

<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.error_notification %>

  <%= f.input :content %>
  <%= f.input :rating %>

  <div class="text-center">
      <%= f.button :submit, class: "btn btn-primary btn-head" %>
    <% end %>
  </div>
</div>
